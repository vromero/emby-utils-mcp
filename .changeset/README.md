# Changesets

This directory holds change metadata for the @emby-utils/server package. See
the [Changesets docs](https://github.com/changesets/changesets) for usage.

Add a new change: `npx changeset`.
