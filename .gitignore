# Dependencies
node_modules/

# Build artefacts
dist/
coverage/
*.tsbuildinfo

# Logs
*.log
npm-debug.log*

# Environment
.env
.env.local
.env.*.local

# Editor
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea/

# OS
.DS_Store
Thumbs.db

# Test artefacts
*.msw-cache

# Local Docker build artefacts (e.g. @emby-utils/client .tgz staged by
# `npm pack` before the client is published). The `docker/` dir itself stays
# tracked via README.md and .gitkeep.
docker/*.tgz

